#cloud-config
users:
  - name: jenkins
    groups: sudo
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    ssh-authorized-keys:
      - ssh-rsa <вставить открытый ключ - инструкции в readme>
